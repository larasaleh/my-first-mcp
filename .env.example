# This project currently requires no API keys or secrets.
# This file exists as a placeholder to signal good practice —
# if a future tool needs credentials, add the variable name here
# (with no real value), then copy this file to .env and fill it in
# locally. .env is already excluded via .gitignore.

# Example (not currently used):
# EXAMPLE_API_KEY=